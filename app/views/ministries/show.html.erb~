<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @ministry.name %>

</p>

<p>
  <strong>Desc:</strong>
  <%= @ministry.desc %>
</p>

   <% @services.each do |item|%>

  <%= item.name %>
<%= link_to 'Show', item %>
        <%= link_to 'Edit', edit_service_path(item) %>
       <%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %>
<br>

     <% end %>

<%= link_to 'Edit', edit_ministry_path(@ministry) %> |
<%= link_to 'Back', ministries_path %>



<br> 
<br>
<%= link_to 'New Service', new_service_path(:ministry_id => @ministry.id) %>
