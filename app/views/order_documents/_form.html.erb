<%= form_for(@order_document) do |f| %>
  <% if @order_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_document.errors.count, "error") %> prohibited this order_document from being saved:</h2>

      <ul>
      <% @order_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :document_id %><br>
    <%= f.text_field :document_id %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.text_field :order_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
